{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install --prefix server && npm install --prefix web",
  "buildCommand": "npm run build --prefix web",
  "outputDirectory": "web/out",
  "functions": {
    "api/index.ts": {
      "maxDuration": 300,
      "includeFiles": "server/**"
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
